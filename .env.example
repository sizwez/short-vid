# Environment example (safe to commit)
# Copy this to a local .env and fill real values there. DO NOT commit .env.

# Supabase
# - SUPABASE_URL: project base URL (e.g. https://<project>.supabase.co)
# - SUPABASE_KEY: anon or client key used by the app for public operations
# - SUPABASE_ANON_KEY: (optional) anon key
# - SUPABASE_SERVICE_ROLE: (sensitive) service role key for server operations
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE=

# OpenAI
# - OPENAI_KEY / OPENAI_API_KEY: API key for OpenAI SDK
OPENAI_KEY=
OPENAI_API_KEY=

# Paystack
# - PAYSTACK_SECRET: secret key for server-side requests
# - PAYSTACK_PUBLIC_KEY: publishable key used client-side
PAYSTACK_SECRET=
PAYSTACK_PUBLIC_KEY=

# JWT
# - JWT_SECRET: secret used to sign/verify JWTs locally
JWT_SECRET=

# Notes:
# - Use this file as a template. Keep the real `.env` out of version control.
# - If you rename or add keys in code, update this example so teammates know what to provide.
